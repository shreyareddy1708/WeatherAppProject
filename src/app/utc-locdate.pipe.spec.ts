import { UtcLocdatePipe } from './utc-locdate.pipe';

describe('UtcLocdatePipe', () => {
  it('create an instance', () => {
    const pipe = new UtcLocdatePipe();
    expect(pipe).toBeTruthy();
  });
});
